{
  "name": "serve0",
  "version": "0.1.0",
  "description": "A programmable reverse proxy and edge gateway written in TypeScript",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky install",
    "install:all": "npm install && npm install --prefix examples/simple && npm install --prefix examples/embed && npm install --prefix benchmark",
    "install:examples": "npm install --prefix examples/simple && npm install --prefix examples/embed",
    "install:benchmark": "npm install --prefix benchmark",
    "build:core": "cd packages/core && npm run build",
    "build:route53": "cd packages/route53 && npm run build",
    "build:cloudflare": "cd packages/cloudflare && npm run build",
    "build:plugins": "cd packages/auth && npm run build && cd ../balancer && npm run build && cd ../cors && npm run build && cd ../health && npm run build && cd ../logger && npm run build && cd ../metrics && npm run build && cd ../ratelimit && npm run build && cd ../security && npm run build",
    "build": "npm run build:core && npm run build:route53 && npm run build:cloudflare && npm run build:plugins",
    "clean": "rm -rf packages/*/dist",
    "format": "prettier --write \"packages/**/*.{ts,tsx,js,json,md}\" \"examples/**/*.{ts,tsx,js,json,md}\" \"*.{md,json}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,tsx,js,json,md}\" \"examples/**/*.{ts,tsx,js,json,md}\" \"*.{md,json}\"",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:watch": "npm run test:watch --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "docs": "typedoc",
    "docs:build": "typedoc",
    "update": "ncu -u",
    "update:workspaces": "ncu -u --workspaces",
    "update:all": "npm run update && npm run update:workspaces"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{js,json,md}": [
      "prettier --write"
    ]
  },
  "keywords": [
    "reverse-proxy",
    "gateway",
    "typescript"
  ],
  "author": "Serve0 Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mensreversa/serve0.git"
  },
  "bugs": {
    "url": "https://github.com/mensreversa/serve0/issues"
  },
  "homepage": "https://github.com/mensreversa/serve0#readme",
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "typescript-eslint": "^8.46.4",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "npm-check-updates": "^19.1.2",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.3.0",
    "typedoc": "^0.28.14",
    "typescript": "^5.9.3"
  }
}
